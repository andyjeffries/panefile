# Dynamic dependencies of the pf binary on macOS.
# Regenerate: scripts/check-dependencies.sh build/release/bin/pf.app/Contents/MacOS/pf > docs/dependencies-darwin.txt
# See §3.4: every optional feature must arrive as a plugin, not a DT_NEEDED entry.
#
# CoreFoundation and CoreServices arrived with M5's FSEvents watcher. They are
# the macOS half of §7.3's platform seam, not an optional feature — there is no
# directory watching on macOS without them, and they are in the dyld shared
# cache, so they cost no page-in.
#
# QtNetwork is deliberately absent. §10.3 names QLocalServer, and linking
# QtNetwork for it added twenty-eight shared libraries on Arch — OpenSSL,
# Kerberos, libcurl, nghttp2, and libproxy, which embeds a JavaScript
# interpreter — for a Unix domain socket. app/SingleInstance.cpp uses POSIX
# sockets and QSocketNotifier instead.
#
# libarchive is deliberately absent too: §3.4 forbids it as a link-time
# dependency, and fs/LibArchive.cpp resolves its symbols with QLibrary on the
# first archive the user opens.
AppKit
CoreFoundation
CoreServices
DiskArbitration
ImageIO
IOKit
libc++.dylib
libSystem.B.dylib
Metal
OpenGL
QtCore
QtGui
QtWidgets
UniformTypeIdentifiers
