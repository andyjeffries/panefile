# Time to first paint, in ms, for:
#   pf --startup-trace --quit-after-paint <fixture>
# Median of 20 runs after 3 warmups, Release build, against the 2,200-entry
# fixture that scripts/make-fixture.sh generates.
#
# macOS 26.3, Apple silicon, Qt 6.10.2. Recorded at M1.
#
# Roughly 170 ms of this is Qt rather than Panefile: ~77 ms constructing
# QApplication (Cocoa plugin, NSApplication) and ~92 ms populating the font
# database on the first QFontMetrics call. See docs/startup-budget.md for why
# macOS is held to this relative baseline rather than to §11's 80 ms target.
291.6
